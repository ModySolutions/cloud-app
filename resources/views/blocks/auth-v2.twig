if(typeof AuthData === 'undefined') {
  var AuthData = {Settings: {}}
}
AuthData.Settings.Email = '{{ email }}';
AuthData.Settings.Key = '{{ key ?? '' }}';
AuthData.Settings.FirstTime = '{{ first_time ?? '' }}';